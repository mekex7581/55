常见命令

```bash
lscpu 						#查看cpu信息
free -h 					#查看内存信息
lsblk 						#查看分分盘情况
arch						#查看系统架构
uname						#查看内核版本
shutdown -h now				#关机
shutdown -r now				#重启
date						#显示时间
date +%F_%T					#显示年月日
date +%s					#查看截至到目前的时间戳
whoami						#查看目前用户信息
history						#查看历史命令行
---
pwd							#显示当前路径
cp							#复制文件;cp -bi 指覆盖并会询问是否覆盖
mv							#移动文件
rm							#删除文件;rm -rf强制删除
tree						#目录树
mkdir						#创建目录;mkdir -p 连续创建
---3
touch						#创建文件
df -h						#查看文件系统
ln							#ln 原有的文件 自定义的目标链接
ln -s						#软连接的创建ln -s 原有的文件 目标链接
---4、用户和权限管理
reboot						#重启,只有root用户才能执行
useradd	<user>				#可以创建新的Linux用户
usermod	<user>				#可以修改用户属性
userdel	<user>				#可删除 Linux 用户,
id <user>					#可以查看用户的 UID，GID等信息
su - <user>					#切换用户
passwd						#可以修改用户密码,<直接就能使用>
chage <user>				#修改用户密码策略
groupadd					#实现创建组,<可以直接使用>
groupmod					#组属性修改,<groupmod 选项 更改的内容 需要更改的组>
groupdel <user>				#删除组,<-f强制删除>
gpasswd	<user>				#命令，可以更改组密码，也可以修改附加组的成员关系
groupmems					#可以管理附加组的成员关系
chown						#命令可以修改文件的属主，也可以修改文件属组<chown 属主:属组 文件名称>
chmod						#修改文件的权限 <chmod +x/w/r 文件名称>
---5文本处理
vim
head						#显示文件或标准输入的前面行
tail						#查看文件或便准输入的倒数行
cut							#按列抽取文本
wc							#-l统计数行数,-c统计数字节总数
sort						#按顺序排序,-u去重
---6、查找和打包
tr							#转换字符删除和压缩重复字符 -s压缩
find						#实时查找工具
xargs						#参数替换
gzip/gunzip					#zip压缩和解压缩
xz/unxz						#xz压缩和解压缩
zip/unzip					#zip压缩和解压缩
tar							#打包和解包
---7、shell脚本
||
&&
if							#表判断;如果
case						#条件判断,选择定义的判断结果
for							#循环
install						#install功能相当于cp,chmod,chown,chgrp,mkdir等相关工具的集合
--
yum							#包管理,
apt							#包管理
list						#列出所有的包,配合yum和list使用
dpkg						#适用与安装.beb的包 -i
rpm							#rpm -qai nginx查询是否安装有nginx包
--9磁盘和文件系统
lsblk						#查看硬盘信息
fdisk						#分区工具
mkfs						#分区的磁盘上创建文件系统
mount						#挂载
umount						#删除挂载
swap						#虚拟内存,建议关掉
df							#文件系统查看工具
du							#目录统计
dd							#创建文件自定义大小工具
--11.网络属性及相关配置
hostname					#显示主机名称,hostname <名称> 添加临时主机名
hostnamectl hostname <name>	#永久更改名称
ifconfig					#查看设备信息,建议使用 ip 代替
ip							#用于管理和配置网络接口、路由和相关网络设置
ss							#查看网络连接
route						#查看路由表,显示路由信息
nmcli						#网络配置工具
netstat						#网络统计工具
tcpdump -i eth0 -nn icmp	#抓包工具
mtr 192.168.10.254 -n		#跟踪路由
--12.进程-系统性能和计划任务
pstree						#进程树,显示父子进程
ps							#查看当前系统中运行的进程
nice						#调整进程优先级
renice						#调整正在运行的进程优先级
uptime						#查看负载信息
top							#查看实时状态,级任务管理器
free						#查看系统的内存使用情况
lsof						#查看进程打开的文件
kill						#用于发送信号到进程的命令，最常用的是终止进程
at							#一次性任务创建
crontab -e					#创建周期性任务
--13.Linux启动流程和内核管理
grub						#
systemctl					#管理系统和服务管理器
systemctl rescue			#切换至救援模式
systemctl emergency			#切换至紧急模式
systemctl halt				#关机
systemctl reboot			#重启
systemctl suspend			#挂起
systemctl get-default		#查看当前系统的形式
runlevel					#显示运行级别,who -r同是
--14.DNS解析
dig							#解析访问方的DNS,查询DNS记录的命令行工具.它常用于调试和分析DNS问题
rndc						#用于控制bind DNS服务器的命令工具
--15
md5sum						#用于计算和校验文件哈希值的命令
base64						#转换数值编码
openssl						#实现加密和各种安全功能
ssh
sshpass						#可使用明文密码链接
scp							#复制远程文件
sudo
timedatectl					#查看时间同步
```

```bash
#ubuntu查看发行版本
[root@ubuntu2204 ~]# cat /etc/os-release
#CentOS8 查看发行版本
[root@centos8 ~]#cat /etc/os-release
```

vim文本编辑实用命令--到.vimrc中定义可持久生效

```bash
set nu				#显示行号
set ai				#自动缩进
```

 bash -nA 运行脚本时可做检查

date +%F --date="+ day"		一天后

()在里面定义内容只能影响在括号里面的内容,而{}可以影响外面

```
linux查看内存利用率 : free -h
linux查看cpu负载量: uptime 、top
linux查看硬盘和分区信息: lsblk
linux查看分区使用情况: df -h
linux查看网络信息: ifconfig
linux如何修改网络信息: 
- centos: vim , nmcli , nmtui
- ubuntu: vim , nmtui
linux查看文件路径: which <name>
linux查看cpu信息: lscpu
linux查看内核版本: uname -a
linux查看当前路径: pwd
Linux查看历史
```

